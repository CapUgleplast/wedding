export const queryKeys = {
  contacts: {
    default: (slug: string) => ['contacts', slug],
  },
  footer: {
    default: (slug: string) => ['footer', slug],
  },
  header: {
    default: (slug: string) => ['header', slug],
  },
  landing: {
    default: (slug: string) => ['landing', slug],
  },
  design: {
    default: (slug: string) => ['design', slug],
  },
  fos: {
    default: (slug: string) => ['fos', slug],
  },
  document: {
    default: (slug: string) => ['document', slug],
  },
}
